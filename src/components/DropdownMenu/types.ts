import type { PopoverPlacement, PopoverStrategy, PopoverTriggerType } from '../Popover'

export type DropdownMenuTriggerType = PopoverTriggerType
export type DropdownMenuPlacement = PopoverPlacement
export type DropdownMenuStrategy = PopoverStrategy
